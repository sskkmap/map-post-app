{
  "name": "bubble-share",
  "version": "1.0.0",
  "description": "Proxy package.json to run ai-story-library",
  "scripts": {
    "dev": "cd ai-story-library && npm run dev",
    "build": "cd ai-story-library && npm run build",
    "start": "cd ai-story-library && npm run start"
  }
}